spring:
  datasource:
    url: jdbc:h2:file:./test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
  profiles:
    active:
logging:
  level:
    org.hibernate:
      SQL: debug
      type.descriptor.sql: trace